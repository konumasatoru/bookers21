<% @books.each do |book| %>
<div>
  <p>投稿ユーザー画像：<%= image_tag '142_refileDownloadImage.png' %></p>
  タイトル：<%= link_to book_path(book.id) do %>
    <%= book.title %>
  <% end %>
  <p>説明：<%= book.body %></p>
  <p>ユーザーネーム：<%= book.user.name %></p>
</div>
<% end %>